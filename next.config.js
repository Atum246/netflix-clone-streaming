/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['image.tmdb.org', 'rb.gy', 'raw.githubusercontent.com'],
  },
  reactStrictMode: true,
}

module.exports = nextConfig